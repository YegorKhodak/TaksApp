@model Task2.Models.ViewModels.CategoriesViewModel
@{
    ViewData["Title"] = "Index";
}
@*<input type="hidden" class="jCurrentUserId" value="@Model.CurrentUserId"/>*@
    <button class="btn btn-info">@Html.ActionLink("Go to Orders", "ForUser", "Orders", new { userId = Model.CurrentUserId })</button>
<h1>Categories</h1>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var category in Model.Categories)
        {
            <tr>
                <td>@category.Id</td>
                <td>@category.Name</td>
                <td>
                    <button class="btn btn-info">@Html.ActionLink("Show products", "Index", "Products", new { userId = Model.CurrentUserId, categoryId = category.Id })</button>
                </td>

            </tr>
        }
    </tbody>
</table>


